# 🛡️ AML Fraud Detection Web App

A comprehensive, full-stack **AML Fraud Detection Platform** demonstrating a strong passion for the financial industry and proactive problem-solving. Built adhering to full **Software Development Life Cycle (SDLC)** practices using Git for version control, this platform leverages advanced **algorithms and data structures** (NetworkX graph analytics) and **Machine Learning** (LightGBM) to detect sophisticated financial fraud typologies in real-time.

It features a resilient dual-database architecture using **RDBMS** (SQLite) and **NoSQL** (TinyDB), and incorporates **Artificial Intelligence and Generative AI** concepts for autonomous triage. The system's reliability is ensured through rigorous **functional, system, and integration testing**, highlighted by a UI automation suite built with **Selenium and BDD frameworks**.

---

## ✨ Features

- **Proactive Problem-Solving via ML** — Real-time transaction scoring using an optimized LightGBM model to instantly predict fraud probabilities.
- **Advanced Algorithms & Data Structures** — Velocity and degree-ratio features (originator/destination history) engineered on-the-fly using NetworkX graph analytics.
- **Generative AI Copilot & SARs** — LangGraph-powered AI agent utilizing LLMs to deeply analyze risk factors and auto-draft FinCEN-compliant Suspicious Activity Reports (SAR).
- **RDBMS + NoSQL Architecture** — Auto-persists structured transaction and SAR data to **SQLite (RDBMS)**, and unstructured investigation/chat data to **TinyDB (NoSQL)**.
- **Selenium BDD Automation Testing** — Comprehensive end-to-end functional and integration testing using **Selenium WebDriver** and **pytest-bdd**.
- **Unified Full-Stack Architecture** — Python FastAPI backend serving a modern JavaScript (React) frontend directly, demonstrating robust software development practices.
- **Premium UI** — Glassmorphism dark theme, animated KPI counters, risk gauge bar, staggered feature animations, and slide-in toast notifications.

---

## 🛠️ Tech Stack

| Layer | Technology |
|---|---|
| **Backend** | Python · FastAPI · Uvicorn |
| **Databases** | SQLite (SQLAlchemy ORM) · TinyDB |
| **ML Model** | LightGBM · scikit-learn · pandas · NetworkX |
| **AI / Agent** | LangGraph · Generative AI (LLMs) |
| **Frontend** | React 19 · Vite 8 |
| **Styling** | Tailwind CSS v4 · custom glassmorphism theme |
| **Testing** | pytest · pytest-bdd · Selenium WebDriver |
| **Icons** | Lucide React |
| **API Docs** | Swagger UI (auto-generated by FastAPI) |

---

## 📁 Project Structure

```
aml-creditcard/
├── frontend/                       # React + Vite app
│   ├── src/
│   │   ├── components/
│   │   │   ├── Sidebar.jsx         # Navigation + API status indicator
│   │   │   ├── KpiCard.jsx         # Animated metric card
│   │   │   ├── RiskGauge.jsx       # Animated probability gauge bar
│   │   │   ├── FeatureGrid.jsx     # Staggered feature value display
│   │   │   └── Toast.jsx           # Slide-in toast notifications
│   │   ├── pages/
│   │   │   ├── Overview.jsx        # Dashboard KPIs + model info
│   │   │   └── Scorer.jsx          # Transaction scoring form + results
│   │   ├── api.js                  # Centralized fetch client
│   │   ├── utils.js                # Animated counter hook + helpers
│   │   ├── App.jsx                 # Root layout + navigation state
│   │   └── index.css               # Tailwind + dark theme tokens
│   └── vite.config.js              # Vite config with FastAPI proxy
│
├── src/aml_detector/               # Core Python package
│   ├── agent.py                    # Agentic logic & Copilot integration
│   ├── api.py                      # FastAPI endpoints + static file serving
│   ├── config.py                   # Project configs & risk thresholds
│   ├── database.py                 # RDBMS (SQLite) & NoSQL (TinyDB) logic
│   ├── data_loader.py              # Dataset loading and caching
│   ├── eda.py                      # Exploratory Data Analysis utilities
│   ├── features.py                 # Feature engineering pipeline
│   ├── file_parser.py              # Parse uploaded batch files
│   ├── graph.py                    # Graph-based feature calculations
│   ├── models.py                   # LightGBM training & prediction
│   ├── schemas.py                  # Pydantic models for API validation
│   └── stream_simulator.py         # Simulates real-time transaction streams
│
├── scripts/
│   ├── run_app.py                  # Unified launcher (dev + prod modes)
│   ├── run_api.py                  # Uvicorn server entrypoint
│   ├── run_modeling.py             # Train the LightGBM model
│   └── run_eda.py                  # Generate EDA reports
│
├── selenium_tests/                 # Selenium BDD test suite
│   ├── features/                   # Gherkin scenario files
│   └── step_defs/                  # Python step implementations
├── tests/                          # Pytest unit & integration test suite
├── outputs/                        # Model metrics + EDA logs
├── requirements.txt
└── setup.py
```

---

## 🚀 Getting Started

### Prerequisites
- Python 3.10+
- Node.js 18+

### 1. Clone & Set Up Python Environment

```bash
git clone https://github.com/hardik1712/aml-creditcard.git
cd aml-creditcard

# Create and activate virtual environment (Windows)
python -m venv venv
.\venv\Scripts\activate

# Install Python dependencies
pip install -r requirements.txt
python setup.py install
```

### 2. Install Frontend Dependencies

```bash
cd frontend
npm install
cd ..
```

### 3. Run the App

**Development mode** — React hot reload + FastAPI running simultaneously:
```bash
python scripts/run_app.py
```
- Frontend (hot reload): `http://localhost:5173`
- Backend API:           `http://127.0.0.1:8000`
- API Docs (Swagger):    `http://127.0.0.1:8000/docs`

**Production mode** — Build React once, FastAPI serves it:
```bash
cd frontend && npm run build && cd ..
python scripts/run_app.py --prod
```
- Unified app: `http://127.0.0.1:8000`

---

## 🧪 Running Tests

```bash
# Run unit and integration tests
pytest tests/ -v

# Run Selenium BDD UI automation tests (requires running app)
pytest selenium_tests/ -v --headed
```

---

## 🧠 Retraining the Model

```bash
python scripts/run_modeling.py
```

This will:
1. Load and clean the PaySim dataset.
2. Engineer graph and velocity features.
3. Train a LightGBM model with early stopping and class-weight balancing.
4. Save the model artifact to `models/model.joblib`.
5. Export evaluation metrics to `outputs/model_metrics.json`.

---

## 📡 API Reference

| Method | Endpoint | Description |
|---|---|---|
| `GET` | `/health` | Health check + model readiness |
| `POST` | `/predict` | Score a single transaction |
| `POST` | `/predict/batch` | Score a batch of transactions |
| `POST` | `/agent/investigate` | Trigger deep investigation & SAR drafting |
| `POST` | `/agent/chat` | Interact with Compliance Copilot |
| `POST` | `/upload` | Score a batch CSV/Excel file |
| `GET` | `/db/transactions` | Query SQLite RDBMS for transactions |
| `GET` | `/db/investigations` | Query TinyDB NoSQL for reports |
| `GET` | `/model/info` | Model type, features, risk thresholds |
| `GET` | `/model/metrics` | Training evaluation metrics |
| `GET` | `/docs` | Interactive Swagger UI |

---

*Built with FastAPI + React + LightGBM.*
